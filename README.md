# Sand Grain Size Mapping System  (SIH & MPMC project)

## About
A project to analyze and map sand grain size using (your method: e.g. image processing + ML). This repository contains code to train models, run inference, and utilities used during development.

## Features
- Training pipeline (scripts in `train/`)
- Inference API / runner (`app.py`)
- Model storage (in `model/`) — **note:** large model files should be stored using Git LFS or cloud storage.
- Core source code in `src/`

